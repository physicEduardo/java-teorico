package org.example.solid;

/*
* Means define the direction of the dependencies, the extended classes should not interact between them
* instead should interact with the abstraction. The abstractions should not depend on details.
* the correct use of interfaces/abstract classes
* */
public class EDependencyIvertion {
}
