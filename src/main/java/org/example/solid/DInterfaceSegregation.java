package org.example.solid;

/*
* This principle means keep things separated, not have a single big common interface instead separated it
* into many interfaces in that way implemented classes doesn't require to implement unneeded methods
*
* */
public class DInterfaceSegregation {
}
